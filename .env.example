# Database - Use transaction pooler (port 6543) for better connection stability
DATABASE_URL=postgres://postgres:Lexydog2023@db.hknfecbyfwqegsnpmbqb.supabase.co:6543/postgres

# Session
SESSION_SECRET=your_session_secret_here

# Google Gemini API
GOOGLE_GEMINI_API_KEY=your_gemini_api_key_here

# Email Configuration (for admin investment updates)
# For Gmail: Enable 2-factor authentication and use an App Password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here

# Environment
NODE_ENV=development
USE_PG_SESSION=false

# Plaid Sync Scheduler
# Set to 'false' to disable the scheduler entirely (default: enabled)
PLAID_SYNC_ENABLED=true
# Run sync immediately on server startup (default: false in dev, true in prod)
PLAID_SYNC_RUN_ON_STARTUP=false
# Days between automatic syncs (default: 30)
PLAID_SYNC_INTERVAL_DAYS=30
